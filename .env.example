# Database — PostgreSQL connection string
# Local dev: spin up with docker-compose up -d
DATABASE_URL="postgresql://jail_ministry:password@localhost:5432/jail_ministry"

# NextAuth — generate with: openssl rand -base64 32
AUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Email — get a free key at resend.com
RESEND_API_KEY="re_..."
EMAIL_FROM="Jail Ministry <noreply@yourdomain.com>"

# SMS (optional — Twilio)
# TWILIO_ACCOUNT_SID=""
# TWILIO_AUTH_TOKEN=""
# TWILIO_PHONE_NUMBER=""
